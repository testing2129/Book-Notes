<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Book Review</title>
  <%- include('favicon') %>
  <link rel="stylesheet" href="/public/notion.css">
  <script src="/public/notion-navbar.js" defer></script>
</head>
<body>
  <%- include('header') %>
  <main>
    <div class="notion-container">
      <h1>Edit Book Review</h1>
      <form action="/edit/<%= book.id %>" method="POST" autocomplete="off" class="notion-form">
        <div>
          <label for="title">Title:</label>
          <input type="text" id="title" name="title" value="<%= book.title %>" required>
        </div>
        <div>
          <label for="author">Author:</label>
          <input type="text" id="author" name="author" value="<%= book.author %>" required>
        </div>
        <div>
          <label for="release_date">Release Date:</label>
          <input type="date" id="release_date" name="release_date" value="<%= book.release_date ? book.release_date.toISOString().slice(0,10) : '' %>" required>
        </div>
        <div>
          <label for="pages">Pages:</label>
          <input type="number" id="pages" name="pages" min="1" value="<%= book.pages %>">
        </div>
        <div>
          <label for="review">Review:</label>
          <textarea id="review" name="review" required><%= book.review %></textarea>
        </div>
        <div>
          <label for="impressions">Impressions:</label>
          <textarea id="impressions" name="impressions"><%= book.impressions %></textarea>
        </div>
        <div>
          <label for="notes">Summary / Notes:</label>
          <textarea id="notes" name="notes"><%= book.notes %></textarea>
        </div>
        <div>
          <label for="rating">Rating (1-5):</label>
          <input type="number" id="rating" name="rating" min="1" max="5" value="<%= book.rating %>" required>
        </div>
        <button type="submit">Save Changes</button>
      </form>
    </div>
  </main>
  <%- include('footer') %>
</body>
</html>
